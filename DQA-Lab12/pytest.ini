[pytest]
testpaths = Tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    unit: fast tests without external services
    integration: tests that call APIs or cloud storage
    ui: Selenium browser tests for Power BI
