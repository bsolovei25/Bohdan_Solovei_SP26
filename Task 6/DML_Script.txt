BEGIN;

-- Default customer SCD row
INSERT INTO BL_3NF.CE_CUSTOMERS_SCD (
    CUSTOMER_ID,
    CUSTOMER_SRC_ID,
    GENDER,
    AGE,
    CUSTOMER_TYPE,
    START_DT,
    END_DT,
    IS_ACTIVE,
    INSERT_DT,
    SOURCE_SYSTEM,
    SOURCE_ENTITY
)
SELECT
    -1,
    'n.a.',
    'n.a.',
    -1,
    'n.a.',
    DATE '1900-01-01',
    DATE '9999-12-31',
    ‘Y’,
    TIMESTAMP '1900-01-01 00:00:00',
    'MANUAL',
    'DEFAULT'
WHERE NOT EXISTS (
    SELECT 1
    FROM BL_3NF.CE_CUSTOMERS_SCD
    WHERE CUSTOMER_ID = -1
);


-- Default satisfaction status row
INSERT INTO BL_3NF.CE_SATISFACTION_STATUSES (
    SATISFACTION_STATUS_ID,
    SATISFACTION_STATUS_SRC_ID,
    SATISFACTION_STATUS,
    INSERT_DT,
    UPDATE_DT,
    SOURCE_SYSTEM,
    SOURCE_ENTITY
)
SELECT
    -1,
    'n.a.',
    'n.a.',
    TIMESTAMP '1900-01-01 00:00:00',
    TIMESTAMP '1900-01-01 00:00:00',
    'MANUAL',
    'DEFAULT'
WHERE NOT EXISTS (
    SELECT 1
    FROM BL_3NF.CE_SATISFACTION_STATUSES
    WHERE SATISFACTION_STATUS_ID = -1
);


-- Default travel row
INSERT INTO BL_3NF.CE_TRAVELS (
    TRAVEL_ID,
    TRAVEL_SRC_ID,
    TRAVEL_TYPE,
    TRAVEL_CLASS,
    FLIGHT_DISTANCE,
    INSERT_DT,
    UPDATE_DT,
    SOURCE_SYSTEM,
    SOURCE_ENTITY
)
SELECT
    -1,
    'n.a.',
    'n.a.',
    'n.a.',
    -1,
    TIMESTAMP '1900-01-01 00:00:00',
    TIMESTAMP '1900-01-01 00:00:00',
    'MANUAL',
    'DEFAULT'
WHERE NOT EXISTS (
    SELECT 1
    FROM BL_3NF.CE_TRAVELS
    WHERE TRAVEL_ID = -1
);


-- Default booking channel row
INSERT INTO BL_3NF.CE_BOOKING_CHANNELS (
    BOOKING_CHANNEL_ID,
    BOOKING_CHANNEL_SRC_ID,
    BOOKING_CHANNEL,
    INSERT_DT,
    UPDATE_DT,
    SOURCE_SYSTEM,
    SOURCE_ENTITY
)
SELECT
    -1,
    'n.a.',
    'n.a.',
    TIMESTAMP '1900-01-01 00:00:00',
    TIMESTAMP '1900-01-01 00:00:00',
    'MANUAL',
    'DEFAULT'
WHERE NOT EXISTS (
    SELECT 1
    FROM BL_3NF.CE_BOOKING_CHANNELS
    WHERE BOOKING_CHANNEL_ID = -1
);


-- Default payment method row
INSERT INTO BL_3NF.CE_PAYMENT_METHODS (
    PAYMENT_METHOD_ID,
    PAYMENT_METHOD_SRC_ID,
    PAYMENT_METHOD,
    INSERT_DT,
    UPDATE_DT,
    SOURCE_SYSTEM,
    SOURCE_ENTITY
)
SELECT
    -1,
    'n.a.',
    'n.a.',
    TIMESTAMP '1900-01-01 00:00:00',
    TIMESTAMP '1900-01-01 00:00:00',
    'MANUAL',
    'DEFAULT'
WHERE NOT EXISTS (
    SELECT 1
    FROM BL_3NF.CE_PAYMENT_METHODS
    WHERE PAYMENT_METHOD_ID = -1
);


-- Default loyalty row
INSERT INTO BL_3NF.CE_LOYALTIES (
    LOYALTY_ID,
    LOYALTY_SRC_ID,
    LOYALTY_LEVEL,
    INSERT_DT,
    UPDATE_DT,
    SOURCE_SYSTEM,
    SOURCE_ENTITY
)
SELECT
    -1,
    'n.a.',
    'n.a.',
    TIMESTAMP '1900-01-01 00:00:00',
    TIMESTAMP '1900-01-01 00:00:00',
    'MANUAL',
    'DEFAULT'
WHERE NOT EXISTS (
    SELECT 1
    FROM BL_3NF.CE_LOYALTIES
    WHERE LOYALTY_ID = -1
);


-- Default travel insurance row
INSERT INTO BL_3NF.CE_TRAVEL_INSURANCES (
    TRAVEL_INSURANCE_ID,
    TRAVEL_INSURANCE_SRC_ID,
    TRAVEL_INSURANCE,
    INSERT_DT,
    UPDATE_DT,
    SOURCE_SYSTEM,
    SOURCE_ENTITY
)
SELECT
    -1,
    'n.a.',
    'n.a.',
    TIMESTAMP '1900-01-01 00:00:00',
    TIMESTAMP '1900-01-01 00:00:00',
    'MANUAL',
    'DEFAULT'
WHERE NOT EXISTS (
    SELECT 1
    FROM BL_3NF.CE_TRAVEL_INSURANCES
    WHERE TRAVEL_INSURANCE_ID = -1
);



COMMIT;
